# Applies the dimension fix to worlds with the following names
#
# To not apply the fix to any worlds, use:
# apply-to-worlds: []
# Format:
# apply-to-worlds:
#   world_folder_name: dimension_type
# The dimension_type must be one of: end, nether
apply-to-worlds:
  end: end
  nether: nether